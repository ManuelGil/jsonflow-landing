# ------------------------------------------------------
# Project environment configuration
# ------------------------------------------------------
# Context-only environment for this repository.
#
# - No runtime loading
# - No PATH mutation
# - No shell-specific behavior
# - Astro manages .env files itself
# ------------------------------------------------------

export ASTRO_PROJECT=true
export NODE_ENV=development
